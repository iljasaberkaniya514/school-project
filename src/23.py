def calculate_average(numbers):
    total = sum(numbers)
    average = total / len(numbers)
    return average

school_project_code = """
def average(numbers):
    total = sum(numbers)
    average = total / len(numbers)
    return average
"""
